doctype html
html(lang='en')
    include ../common/head.pug
    +head("Edit Leads")
    body
        form(action="/lead/"+lead.id+"/edit",method="POST")
            input(name="lead_email" type="email",placeholder="Enter new Email Id:" value=lead.email)
            button(type="submit") Save
        include ../common/footer.pug
        +footer()